const db = require("quick.db");
const score = require("./script.js");
var logonFailedAlert = document.getElementById("alert");

$(document).ready(function () {
  //$("#timerBar").hide(); would hide an element with the ID timerBar
});
